# if you run this program without arguments, the `gets` will get whatever you type
# if you run it with a file as an argument, it will use it as the source to `gets`
while line = gets
  puts line
end
